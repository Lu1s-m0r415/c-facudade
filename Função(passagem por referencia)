#include <iostream>
#include <string>

using namespace std;

// s é passada a função por referencia
void soma(int x, int y, int &s)
{
	s = x + y;
}

int main()
{
	int x, y;
	int s = 0;
	
	cin >> x >> y;
	
	soma(x, y, s);
	
	cout << "s = " << s << endl;
	
	return 0;
}
